<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Calendar invitation</title>@vite(['resources/css/app.css'])</head><body><main class="guest-calendar-card"><h1>{{ $attendee->event->title }}</h1><p>{{ $attendee->event->starts_at->setTimezone($attendee->event->timezone)->format('D, d M Y g:i A T') }}</p>@if(session('status'))<p>{{ session('status') }}</p>@endif<form method="POST" action="{{ request()->fullUrl() }}">@csrf @foreach(['accepted'=>'Accept','tentative'=>'Tentative','declined'=>'Decline'] as $value=>$label)<button name="response" value="{{ $value }}" type="submit">{{ $label }}</button>@endforeach</form></main></body></html>
